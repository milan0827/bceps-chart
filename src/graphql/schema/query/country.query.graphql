query Country($code: ID!) {
  country(code: $code) {
    code
    currency
    emoji
    emojiU
    languages {
      code
      name
      native
      rtl
    }
    name
    continent {
      code
      name
    }
    phone
    states {
      code
      name
    }
  }
}
